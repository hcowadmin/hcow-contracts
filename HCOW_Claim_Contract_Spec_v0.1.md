# HCOW 클레임 컨트랙트 사양서 v0.1

> 작성 2026-09-16 · 대상: 구현 담당 (Claude Code)
>
> **이 문서를 읽는 개발자에게**
> 이 컨트랙트는 이미 감사가 끝나고 배포를 앞둔 시스템에 붙는 부품이다.
> 기존 컨트랙트는 **절대 수정하지 않는다.** 이 컨트랙트는 완전히 독립적으로 추가된다.
> 사양에 없는 기능을 추가하지 않는다. 판단이 필요하면 구현하지 말고 먼저 물어볼 것.

---

## 1. 배경 (이걸 모르면 잘못 만든다)

### 우리가 누구인가

HashCow(HCOW). BNB Chain 위의 BEP-20 토큰. 총 발행량 200,000,000 고정, mint 함수 없음.
컨트랙트 6개가 SolidProof 감사를 2회 받았고 **Critical 0 / High 0 / Medium 0**로 종료했다.
2차 보고서를 미해결 항목 포함 전문 공개했다. **검증 가능성이 이 프로젝트의 핵심 포지셔닝이다.**

### 왜 이 컨트랙트가 필요한가

`HCOWVesting`은 **9개의 수혜자 주소(트랜치)** 에 대해 베스팅을 건다. 개별 유저가 아니라 버킷 단위다.

```
Public               60,000,000   TGE 15%    클리프 0   선형 12개월
Private              40,000,000   TGE 7.5%   클리프 6   선형 12개월
Seed                 20,000,000   TGE 25%    클리프 0   선형 3개월
Community/Airdrop     8,000,000   TGE 37.5%  클리프 0   선형 6개월   ← 이 버킷이 대상
Community/Incentives 12,000,000   TGE 0%     클리프 0   선형 24개월
Ecosystem & R&D      20,000,000   TGE 0%     클리프 6   선형 42개월
Foundation           16,000,000   TGE 0%     클리프 12  선형 36개월
Liquidity            14,000,000   TGE 50%    클리프 0   선형 12개월
Team                 10,000,000   TGE 0%     클리프 12  선형 36개월
```

**`Community/Airdrop` 버킷(8,000,000)의 수혜자 주소를 이 클레임 컨트랙트로 지정한다.**
즉 온체인 베스팅이 이 컨트랙트로 토큰을 흘려보내고, 개별 유저는 이 컨트랙트에서 청구한다.

### 절대 건드리면 안 되는 것

- **`HCOWVesting`, `HCOWToken` 등 기존 6개 컨트랙트를 수정하지 않는다.** 감사가 끝났다
- `beneficiaryCount()`는 봉인 전 정확히 **9**를 읽어야 한다. 스케줄을 늘리지 않는다
- 9행 합계가 총 발행량과 정확히 같아서 **1 wei 소각에도 봉인이 영구 불가능해진다**
- `tgeTime`은 immutable이다

---

## 2. 무엇을 만드는가

**다회차 머클 배포 컨트랙트 1개 + 머클 트리 생성 스크립트 + 테스트.**

### 대상자 규모

```
약속 잔고 기록          20,571 건 (HcowPromisedLedger)
미니앱 총 유저          약 13,700 명
지갑 등록률             6.3% (866명)   ← 대부분이 아직 받을 주소가 없다
```

### 수령 방식

유저가 웹(`app.hash-cow.io`)에서 지갑을 연결하고 직접 청구한다. **푸시 전송이 아니다.**
가스는 유저 부담이다. 다만 **소액 수령자가 압도적 다수**이므로 회차를 잘게 쪼개면
회차당 금액이 가스비보다 작아져 아무도 청구하지 않는 상황이 생긴다. 4장 최소 청구 수량 참조.

---

## 3. 언락 정책 (변경 가능 — 코드에 박지 말 것)

에어드랍은 종류별로 다른 언락 곡선을 갖는다.

| 종류 | TGE 지급 | 이후 | 총 회차 |
|---|---|---|---|
| 미니앱 보상 | 33% | 2개월 분할 | 3 |
| TaskOn | 20% | 4개월 분할 | 5 |
| 인식개선 프로그램 | 100% | 없음 | 1 |
| 빌드페이즈 | 25% | 3개월 분할 | 4 |
| 기타 소액 보상 | 100% | 없음 | 1 |

**이 수치는 확정되지 않았고 바뀐다. 컨트랙트에 하드코딩하지 말 것.**

컨트랙트는 "이 주소가 이 회차에 얼마를 받을 수 있는가"만 검증한다.
종류별 곡선은 **머클 트리를 만드는 단계에서 계산되어 수량으로 들어간다.**
따라서 정책이 바뀌어도 컨트랙트는 바뀌지 않는다. 이 분리를 반드시 지킬 것.

### 개월의 정의

**모든 개월은 30일이다.** `HCOWVesting.sol`의 `MONTH` 상수와 동일한 정의를 쓴다.
달력 월이 아니다. 온체인 베스팅과 회차가 어긋나면 컨트랙트에 토큰이 없는데 청구가 열린다.

### 나머지 처리

33%를 3회차로 나누면 33 / 33.5 / 33.5가 되어 정수로 안 떨어진다.
**마지막 회차가 나머지를 전부 받는다.** 트리 생성 스크립트가 이를 보장하고,
각 회차 수량 합계 = 총 수량이 되도록 검증한다.

---

## 4. 기능 요구사항

### 4-1. 회차(Round)

- 회차는 순번(`uint256 roundId`)으로 식별한다
- 각 회차는 **머클 루트 1개**와 **개시 시각 1개**를 갖는다
- 개시 시각 이전에는 청구 불가
- 여러 회차가 동시에 열려 있을 수 있다. 유저가 밀린 회차를 한 번에 청구할 수 있어야 한다

### 4-2. 청구

```
claim(roundId, index, account, amount, merkleProof)
claimMany(roundId[], index[], account[], amount[], merkleProof[][])   // 여러 회차 일괄
```

- proof가 유효하지 않으면 revert
- 이미 청구한 (roundId, account) 조합이면 revert
- 회차가 아직 열리지 않았으면 revert
- 컨트랙트 잔액이 부족하면 **claimed를 남기지 않고 전체 revert**

### 4-3. 최소 청구 수량

```
minClaimAmount   (설정값, 초기값 미정)
```

**정책은 아직 미정이다. 파라미터로 만들고 초기값은 0으로 둔다.**
0이면 제한 없음. 나중에 owner가 설정할 수 있게 한다.

의도: 총 수령량이 극소액인 계정은 여러 회차로 나누면 회차당 금액이 가스비보다 작아진다.
이 경우 트리 생성 단계에서 전액을 1회차에 몰아주는 방식으로 처리하는 것이 1안이고,
컨트랙트 레벨에서 막는 것이 2안이다. **1안을 기본으로 구현하고 파라미터는 남겨둔다.**

### 4-4. 루트 관리

```
setRoot(roundId, merkleRoot, startTime)     // 회차 등록
```

**한 번 개시 시각이 지난 회차의 루트는 변경할 수 없다.** 반드시 이 제약을 넣을 것.

이유: 루트를 언제든 바꿀 수 있으면 운영자가 임의의 주소에 임의의 금액을 줄 수 있다는 뜻이 된다.
커뮤니티가 코드를 읽으면 즉시 지적한다. **이 프로젝트는 그 지적을 감당할 수 없다.**

아직 개시하지 않은 회차의 루트는 수정 가능해도 된다. 그 경우 이벤트를 남길 것.

### 4-5. 회수

```
sweep(to, amount)    // 미청구 물량 회수
```

- **클레임 기한(`claimDeadline`)이 지난 뒤에만 호출 가능**
- 기한은 배포 시 설정하고, **연장은 가능하되 단축은 불가능**해야 한다
- 회수 시 이벤트 필수

기한과 회수 대상 주소는 미정이다. 파라미터로 둔다.

**기한 단축을 막는 이유**: 단축이 가능하면 "청구 기간을 갑자기 줄여 물량을 몰수했다"가 성립한다.
이 프로젝트는 최근 파트너의 유사한 행위를 공개 비판했다. 같은 구조를 우리가 가지면 안 된다.

### 4-6. 이벤트

```
RoundSet(roundId, merkleRoot, startTime)
Claimed(roundId, index, account, amount)
Swept(to, amount)
DeadlineExtended(oldDeadline, newDeadline)
```

### 4-7. 권한

- `setRoot`, `sweep`, `extendDeadline`은 owner 전용
- **owner는 멀티시그(Safe) 주소가 된다.** 배포 시 인자로 받는다
- owner가 토큰을 임의로 빼갈 수 있는 경로는 `sweep` 하나뿐이어야 하고, 기한 제약이 걸려야 한다

---

## 5. 구현 제약

기존 저장소와 동일하게 맞춘다. 어긋나면 BscScan 검증이 실패한다.

```
Solidity      0.8.34 고정 (caret 범위 아님)
optimizer     200
evmVersion    paris  (명시)
OpenZeppelin  5.0.2
업그레이더블    아님. 프록시 없음
체인          BNB Chain (chain id 56), 테스트넷 97
```

이 설정은 `hardhat.config.cjs`, `foundry.toml`, `compile.cjs` 네 곳에 동일하게 있어야 한다.

### 코드 구조

- **Uniswap MerkleDistributor를 기준 구현으로 삼고 최대한 그대로 쓴다.** 창의적으로 바꾸지 않는다
- `checks-effects-interactions` 순서를 지킨다. **transfer 전에 claimed를 기록**한다
- 회차별 claimed는 비트맵(`mapping(uint256 => mapping(uint256 => uint256))`)으로 구현한다
- 재진입 방지가 필요한지 검토하고, 필요하면 OpenZeppelin `ReentrancyGuard`

---

## 6. 머클 트리 생성 스크립트

**컨트랙트보다 여기서 사고가 더 많이 난다.** 별도 스크립트로 만들고 검증을 붙인다.

### 입력

```
CSV 또는 JSON
  account          지갑 주소
  category         종류 (미니앱/TaskOn/인식개선/빌드페이즈/기타)
  totalAmount      총 약속 수량 (wei)
```

### 처리

1. 종류별 언락 곡선을 적용해 회차별 수량으로 분해한다
2. 나머지는 마지막 회차에 몰아준다
3. 회차별로 머클 트리를 생성한다
4. 루트와 각 계정의 proof를 출력한다

### 필수 검증 (스크립트가 스스로 검사하고, 실패하면 중단)

```
□  각 계정의 회차별 수량 합계 = 그 계정의 totalAmount
□  회차별 전체 수량 합계 = 그 회차에 컨트랙트가 보유할 물량 이하
□  전 회차 총합 = 입력 파일의 총합
□  중복 주소 없음
□  주소 체크섬 유효
□  수량 0인 항목 제외
□  생성된 루트를 독립적인 두 번째 경로로 재계산해 대조
```

**마지막 항목이 중요하다.** 같은 코드로 두 번 계산하는 것은 검증이 아니다.
라이브러리를 바꾸거나 계산 순서를 바꿔 독립적으로 뽑아 비교할 것.

---

## 7. 테스트 (전부 통과해야 배포)

```
1   같은 회차 두 번 청구                    →  revert
2   1회차 청구 후 2회차 청구                 →  성공. 회차 간 간섭 없을 것
3   잘못된 proof                           →  revert
4   올바른 proof에 다른 금액                 →  revert
5   다른 계정의 proof로 청구                 →  revert
6   개시 시각 전 청구                        →  revert
7   이미 개시된 회차의 루트 변경              →  revert
8   개시 전 회차의 루트 변경                  →  성공 + 이벤트
9   잔액 부족 상태에서 청구                   →  revert, claimed가 남지 않을 것
10  amount 0 항목                          →  트리에 없어야 함
11  claimDeadline 전 sweep                 →  revert
12  claimDeadline 후 sweep                 →  성공 + 이벤트
13  deadline 단축 시도                      →  revert
14  deadline 연장                          →  성공 + 이벤트
15  owner 아닌 주소의 setRoot / sweep        →  revert
16  claimMany로 밀린 3개 회차 일괄 청구        →  성공, 합계 정확
```

**9번이 가장 중요하다.** claimed를 먼저 찍고 transfer가 실패하면 그 계정은 영원히 청구할 수 없다.
다회차 배포에서 가장 흔한 영구 손실 사고다.

### 테스트넷 리허설

실제 데이터의 축소판(약 100건)으로 전 회차를 실행한다.
중간에 재청구와 위조 proof 시도를 섞어 실패하는지 확인한다.

---

## 8. 산출물

```
contracts/HCOWClaim.sol
scripts/build-merkle.cjs          트리 생성 + 검증
scripts/deploy-claim.cjs          배포
test/HCOWClaim.t.sol 또는 .test.cjs
README.md                         배포 순서, 회차 등록 절차, 운영 주의사항
```

---

## 9. 배포 순서 (README에 적을 것)

```
1  HCOWClaim 배포 (owner = 트레저리 Safe, claimDeadline 설정)
2  HCOWVesting의 Community/Airdrop 수혜자 주소를 HCOWClaim 주소로 지정
   ※ 이 주소는 봉인 후 영구 변경 불가
3  봉인 (fundAndSeal)
4  TGE 도래 후 베스팅에서 HCOWClaim으로 토큰 유입
5  1회차 머클 루트 등록 (setRoot)
6  클레임 페이지 오픈
7  이후 회차마다 5번 반복
```

**2번이 되돌릴 수 없는 지점이다.** 봉인 전에 HCOWClaim 주소가 확정되어 있어야 한다.
즉 **이 컨트랙트가 봉인보다 먼저 배포되어야 한다.**

---

## 10. 미결 항목 (구현 전에 확인할 것)

1. **종류별 총 수량** — 미니앱 / TaskOn / 인식개선 / 빌드페이즈 / 기타 각각의 합계.
   합계가 `Community/Airdrop` 버킷의 회차별 유입량을 초과하면 정책이 실행 불가능하다
2. **최소 청구 수량** 값
3. **클레임 기한** 길이
4. **회수 대상 주소**
5. 레퍼럴 1% 커미션이 "미니앱 보상"에 포함되는지 별도 종류인지
6. 시즌 1 상금(2,500 HCOW)은 **공개 규칙에 "TGE 시 지급"으로 명시**되어 있으므로
   반드시 TGE 100% 지급이어야 한다. "기타"로 분류하면 맞다

---

## 11. 하지 말 것

```
기존 컨트랙트 6개 수정
언락 비율을 컨트랙트에 하드코딩
개시된 회차의 루트를 변경 가능하게 만들기
claimDeadline 단축 가능하게 만들기
owner가 기한 전에 물량을 빼갈 수 있는 경로 추가
사양에 없는 기능 추가 (스테이킹, 자동 복리, 위임 등)
transfer 후에 claimed 기록 (순서 반대)
```

**판단이 필요한 지점이 나오면 구현하지 말고 먼저 물어볼 것.**
이 프로젝트에서 사고는 코드 품질이 아니라 맥락 손실에서 났다.
